version: 2.1.{build}-{branch}
os:
  - Visual Studio 2015
environment:
  pippo: Pluto
before_build:
  - msbuild HelloWorld.proj /v:diag /logger:"C:\Program Files\AppVeyor\BuildAgent\Appveyor.MSBuildLogger.dll"
build:
  project: HelloWorld.proj